# 이 저장소를 다루는 에이전트에게

먼저 `SETUP.md`와 `style/README.md`를 읽습니다. 이 저장소는 공용 실행기입니다. 프로젝트 코드, 사내 Jira 원문, 인증 정보는 프로젝트의 별도 작업 영역에 둡니다.

문서 사이트를 꾸밀 때는 `DESIGN.md`와 `docs/design.md`도 읽습니다. 기본 예제는 Slack 디자인이며 프로젝트의 `design` 설정으로 교체합니다. 디자인 변경만으로 기술 원고의 검토를 다시 승인하지 않습니다.

- 사용자가 셋업을 요청하면 현재 환경을 읽기 전용으로 확인하고 `examples/camera-hal`을 바탕으로 프로젝트 설정을 만듭니다. 필수 경로·서버·최초 처리 커밋이 없으면 필요한 값만 질문합니다. 선택 연동은 설정하지 않은 상태로 끌 수 있습니다.
- 셋업 에이전트는 Claude, Hermes 등 어느 것이어도 됩니다. 문서 실행 에이전트와 모델은 프로젝트의 `agent` 설정을 따릅니다. 사용자의 Qwen 설정을 클라우드 모델로 대체하지 않습니다.
- `npm test`, 프로젝트 `doctor`, `sync --dry-run`, 임시 프로젝트의 실행·복구 검사를 완료합니다. 사내 서비스 실접속을 검사하지 못했으면 완료했다고 보고하지 않습니다.
- `sync`는 검토 승인과 checkpoint를 갱신하지 않습니다. `verify --accept`는 코드를 대조한 검토를 기록하며, `ack`는 문서 반영 완료 후 실행합니다.
- 미러링과 문서 작업은 성공 후 순서대로 연결합니다. 기존 17:00 미러링과 경쟁하는 별도 cron을 만들지 않습니다.
- 모든 설명 문서와 생성 원고에 i-have-adhd와 fluent-korean 원문 규칙을 적용합니다. 기술적 조건과 미확인 사항을 줄여 쓰지 않습니다.
- 공용 코드를 바꿨으면 테스트 후 버전과 배포 기록을 갱신합니다. 프로젝트는 검증된 버전의 공용 엔진을 별도로 설치해 호출합니다.
