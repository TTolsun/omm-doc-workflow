# Architecture Intelligence

**코드 → 구조 모델 → 문서를 탐색하는 엔지니어링 화면을 만듭니다.** 전체 시각적 방향은 Together AI 60%, 탐색 구조는 Linear 25%, 코드 근거 블록은 Warp 15%를 참고합니다. 비율은 시각적 우선순위이며 측정값이 아닙니다.

## 화면의 우선순위

1. 어떤 시스템과 관점을 보고 있는지 먼저 보여 줍니다.
2. 구조도와 코드 근거를 같은 화면에서 탐색하게 합니다.
3. 검토 기준과 현재 코드의 일치 여부를 명확히 표시합니다.
4. 긴 설명은 밝은 바탕에서 읽고, 구조도와 근거는 어두운 작업 영역에 배치합니다.
5. 변경 기록과 미확인 사항을 실제 데이터가 있을 때만 보여 줍니다.

## 시각적 규칙

| 영역 | 적용 방식 |
| --- | --- |
| 상단과 구조도 | 잉크색 배경, 얇은 격자와 구분선, 주황·마젠타·연보라의 제한된 강조를 사용합니다. |
| 문서 탐색 | 작은 모서리, 좁고 일정한 간격, 분명한 현재 위치 표시를 사용합니다. |
| 코드 근거 | 파일명·커밋·상태를 고정폭 글꼴의 블록으로 정리합니다. |
| 본문 | 밝은 배경, 짧은 문단, 명확한 표와 접을 수 있는 상세 근거를 사용합니다. |
| 글꼴 | Geist·Geist Mono를 우선 지정하고 설치되어 있지 않으면 한국어 시스템 글꼴과 고정폭 글꼴로 대체합니다. |

상용 글꼴이나 외부 폰트를 자동으로 내려받지 않습니다. 모서리는 기본 4px, 경계선은 1px로 설정합니다. 색상만으로 상태를 구분하지 않으며 키보드 초점, 모바일 탐색, 그림 확대, 동작 줄이기 설정을 유지합니다.

## 데이터 표시 원칙

- `SOURCE FILES`는 바인딩과 원고 인용에서 확인한 중복 없는 근거 파일 수입니다. 실제 LLM이 분석한 파일 수라고 바꿔 쓰지 않습니다.
- `REVIEWED`는 기록된 검토 날짜입니다. 스캔 실행 시각으로 표시하지 않습니다.
- `CODE REVISION`은 기록된 검토 커밋입니다. Gerrit 리비전이 없으면 Gerrit이라고 표시하지 않습니다.
- `REVIEWED ITEMS`는 검토 기준과 일치하는 항목 수이며, 모델의 정확도나 신뢰도 확률이 아닙니다.
- 최신성이 확인되지 않았으면 녹색 동기화 표시를 사용하지 않습니다. 수집 자료가 없으면 미확인 상태를 표시합니다.

## 기능 범위

현재 화면은 근거 파일 검색, 코드 링크, 문서 목차 이동, 구조도 확대, 요소 이름 검색을 제공합니다. 이름 검색은 문자열 일치로 노드를 강조하며 자연어 경로 추론을 수행하지 않습니다.

`Ask Architecture`와 질문에 따른 경로 재구성은 후속 기능입니다. 근거 인용이 가능한 질의 API, 접근 권한, 구조 그래프의 안정적인 ID, 답변 평가를 갖춘 뒤 연결합니다. 동작하지 않는 AI 입력창이나 가상의 신뢰도·변경 건수를 표시하지 않습니다.

실제 Camera HAL의 BYRP·RGBP·MCSC 같은 컴포넌트는 해당 코드가 연결된 프로젝트에서만 표시합니다. HALCamera 앱에는 실제 앱의 구조를 사용합니다. i-have-adhd와 fluent-korean 집필 규칙은 계속 적용합니다.

## 참고 자료

- Together AI: https://getdesign.md/together.ai/design-md
- Linear: https://getdesign.md/linear.app/design-md
- Warp: https://getdesign.md/warp/design-md

getdesign으로 설치한 각 참고 원문은 `designs/together-ai`, `designs/linear`, `designs/warp`에 보관합니다. Slack은 이전 테마 선택지로 유지합니다.
