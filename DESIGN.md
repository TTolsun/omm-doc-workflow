# 읽기 중심의 개발자 문서 디자인

**가독성 → 깔끔함 → 친숙함 → 혁신성 순서로 판단합니다.** 기본 프리셋은 `reading`입니다. Mintlify 70%, Vercel 20%, GitBook 10%는 시각적 우선순위를 뜻하며 측정값이 아닙니다.

## 문서 구조

왼쪽에는 주제별 문서 탐색, 가운데에는 본문, 오른쪽에는 현재 페이지 목차를 배치합니다. 본문은 최대 약 720px, 기본 글자 크기는 16px, 한국어 줄 간격은 1.8로 설정합니다. 좁은 화면에서는 목차를 본문 위에서 펼칠 수 있습니다.

페이지는 제목과 실제 검토 상태로 시작하고, 설명 → 구조도 → 실행 흐름 → 인터페이스·조건 → 코드 근거 순으로 구성합니다. 필요한 절만 사용하며 없는 코드나 HAL 컴포넌트를 예시 화면을 위해 만들어 넣지 않습니다. 구조도는 관련 설명 바로 뒤에 둡니다.

## 시각적 규칙

| 항목 | 적용 규칙 |
| --- | --- |
| 배경과 글자 | 흰 배경과 #111 본문을 사용합니다. 보조 글자도 충분한 명도 대비를 유지합니다. |
| 강조 | 짙은 녹색을 링크·현재 위치·정상 상태에 제한적으로 사용합니다. 상태는 문장도 함께 표시합니다. |
| 글꼴 | Geist와 Geist Mono를 우선 지정하고 한국어 시스템 글꼴로 대체합니다. 외부 글꼴을 자동으로 내려받지 않습니다. |
| 경계와 여백 | 1px 경계선, 4–6px 모서리, 충분한 문단 간격을 사용합니다. 일반 카드에 그림자를 넣지 않습니다. |
| 코드와 표 | 코드는 어두운 블록, 짧은 식별자는 연한 회색 배경, 표는 얇은 행 구분선을 사용합니다. |

Mintlify에서는 문서 레이아웃·본문·목차·코드·표를 가져옵니다. Vercel에서는 단색 구성과 시각적 절제를 참고합니다. GitBook에서는 익숙한 주제별 탐색을 참고합니다. Linear의 영향은 작은 상태·메타데이터 표현으로 제한합니다.

마케팅용 대형 hero, 그라데이션, CTA, 후기, 과도한 카드를 넣지 않습니다. 기존의 어두운 [Architecture Intelligence 디자인](designs/architecture/DESIGN.md)은 `architecture` 프리셋으로 유지합니다.

## 실제 정보와 동작

- 상단에는 기록된 검토 상태와 날짜만 표시합니다. 빌드 시간을 분석 시각이나 마지막 검토 시각으로 바꾸지 않습니다.
- 파일 수·구조 관점·검토 항목 수와 근거 파일 검색은 본문 하단에 둡니다. 검토 기준이 일치하는 항목 수는 AI 신뢰도 확률이 아닙니다.
- 문서 찾기는 탐색 메뉴의 제목과 현재 페이지 목차를 검색합니다. 전체 문서 본문 검색이나 자연어 답변 기능으로 표현하지 않습니다.
- 그림을 누르거나 Enter 키를 누르면 확대합니다. 별도의 크게 보기 버튼은 만들지 않습니다. 요소 검색·실제 SVG 확대·Escape 닫기·초점 복원을 유지합니다.
- 이전·다음 링크는 설정된 메뉴 순서를 따릅니다. 가상의 페이지를 생성하지 않습니다. 탭이 필요한 경우 기존 사이트의 접근 가능한 탭 구성 요소를 사용합니다. 이 템플릿은 탭 실행기를 포함하지 않습니다.

문체는 i-have-adhd와 fluent-korean을 적용합니다. 사내 데이터가 없는 화면에서는 실제 분석 결과처럼 표시하지 않습니다. 자연어 Ask Architecture는 후속 기능이며 가짜 입력창을 만들지 않습니다.

## 참고 원문

- [Mintlify getdesign 원문](designs/mintlify/DESIGN.md): 문서 구조의 기준입니다.
- [Vercel getdesign 원문](designs/vercel/DESIGN.md): 단색·글꼴·여백을 참고합니다.
- [GitBook 디자인 설명](https://getdesign.md/design-md/gitbook): 친숙한 문서 탐색을 참고합니다.

getdesign 원문은 외부 참고 자료입니다. 프로젝트에 적용할 규칙은 이 문서와 [디자인 설정](docs/design.md)에서 제한하고 구체화합니다.
